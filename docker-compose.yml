version: "3.9"

services:
  server:
    build: ./server
    container_name: supcontent_server
    ports:
      - "5000:5000"
    restart: always

  web:
    build: ./web
    container_name: supcontent_web
    ports:
      - "3000:3000"
    depends_on:
      - server
    restart: always